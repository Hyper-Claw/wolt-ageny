{
  "name": "arc-launchpad",
  "version": "0.1.0",
  "private": true,
  "description": "arc.fun — a bonding-curve token launchpad for the Arc chain",
  "scripts": {
    "setup": "npm --prefix contracts install && npm --prefix web install",
    "chain": "npm --prefix contracts run node",
    "seed": "npm --prefix contracts run seed:local",
    "web": "npm --prefix web run dev",
    "test": "npm --prefix contracts test",
    "demo": "npm --prefix contracts run demo"
  }
}
