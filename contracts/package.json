{
  "name": "arc-launchpad-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Bonding-curve token launchpad contracts for the Arc chain",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "check:arc-mainnet": "hardhat run scripts/check-env.ts --network arcMainnet",
    "check:arc-testnet": "hardhat run scripts/check-env.ts --network arcTestnet",
    "deploy:arc-testnet": "hardhat run scripts/deploy.ts --network arcTestnet",
    "deploy:arc-mainnet": "hardhat run scripts/deploy.ts --network arcMainnet",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "launch-trade:arc-mainnet": "hardhat run scripts/launch-and-trade.ts --network arcMainnet",
    "launch-trade:arc-testnet": "hardhat run scripts/launch-and-trade.ts --network arcTestnet",
    "seed:local": "hardhat run scripts/seed.ts --network localhost",
    "demo": "hardhat run scripts/demo.ts",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.17",
    "@types/mocha": "^10.0.7",
    "@types/node": "^22.5.0",
    "@uniswap/v3-core": "^1.0.1",
    "@uniswap/v3-periphery": "^1.4.4",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "solc": "^0.8.24",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
